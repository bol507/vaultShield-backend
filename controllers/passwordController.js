const Password = require('../models/Passwords')

const getPassword = async (erq,res)=>{
    const passId = req.password.passId
    try{
        const password = await Password.findById({id: userId})
    }
}